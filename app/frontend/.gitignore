# dependencies
node_modules

# build output
dist
dist-ssr

# local env overrides (the committed base .env stays tracked)
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# editor / OS
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json